apply plugin: 'war'

war {
    baseName = rootProject.name
    version = project.version
}

dependencies {
    providedRuntime 'org.springframework.boot:spring-boot-starter-tomcat'
}

configurations {
    compile.exclude group: "org.springframework.boot", module: "spring-boot-devtools"
}